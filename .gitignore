.dev/
.codex
.claude/
.vercel
site/

# sekrety środowiskowe (przykłady .env.example zostają w repo)
.env
.env.local
.env.*.local

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
